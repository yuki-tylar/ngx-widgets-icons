import { IconHostDirective } from './icon-host.directive';

describe('IconHostDirective', () => {
  it('should create an instance', () => {
    const directive = new IconHostDirective();
    expect(directive).toBeTruthy();
  });
});
